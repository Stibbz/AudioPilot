<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ══ Title-bar buttons ══════════════════════════════════════════════════ -->

    <Style x:Key="TitleBarButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Bg" Background="Transparent" CornerRadius="6">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bg" Property="Background" Value="#2A2A2A" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="CloseButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Bg" Background="Transparent" CornerRadius="6">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bg" Property="Background" Value="#C42B1C" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="IconButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Bg" Background="Transparent" CornerRadius="6">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bg" Property="Background" Value="#252525" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ══ Device card (main switcher) ═══════════════════════════════════════ -->

    <Style x:Key="DeviceCardButtonStyle" TargetType="Button">
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Card" Background="#222222" BorderBrush="#2E2E2E"
                            BorderThickness="1" CornerRadius="10" Padding="14,12">
                        <ContentPresenter />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Card" Property="Background" Value="#2A2A2A" />
                            <Setter TargetName="Card" Property="BorderBrush" Value="#3A3A3A" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Card" Property="Background" Value="#1E1E1E" />
                        </Trigger>
                        <!-- BT disconnected: subtle blue-dark tint -->
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding IsBluetooth}" Value="True" />
                                <Condition Binding="{Binding IsBluetoothConnected}" Value="False" />
                                <Condition Binding="{Binding IsConnecting}" Value="False" />
                                <Condition Binding="{Binding IsConnectionFailed}" Value="False" />
                            </MultiDataTrigger.Conditions>
                            <Setter TargetName="Card" Property="Background" Value="#1A1A26" />
                            <Setter TargetName="Card" Property="BorderBrush" Value="#2E2E44" />
                        </MultiDataTrigger>
                        <!-- Connecting: slightly dimmed -->
                        <DataTrigger Binding="{Binding IsConnecting}" Value="True">
                            <Setter TargetName="Card" Property="Background" Value="#1A1A26" />
                            <Setter TargetName="Card" Property="Opacity" Value="0.7" />
                        </DataTrigger>
                        <!-- Connection failed: red tint -->
                        <DataTrigger Binding="{Binding IsConnectionFailed}" Value="True">
                            <Setter TargetName="Card" Property="Background" Value="#261A1A" />
                            <Setter TargetName="Card" Property="BorderBrush" Value="#6B2020" />
                        </DataTrigger>
                        <!-- Active (default) device -->
                        <DataTrigger Binding="{Binding IsDefault}" Value="True">
                            <Setter TargetName="Card" Property="Background" Value="#162033" />
                            <Setter TargetName="Card" Property="BorderBrush" Value="#4A90D9" />
                            <Setter TargetName="Card" Property="BorderThickness" Value="1.5" />
                        </DataTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Icon bubble background -->
    <Style x:Key="DeviceIconBorderStyle" TargetType="Border">
        <Setter Property="Background" Value="#2A2A2A" />
        <Style.Triggers>
            <DataTrigger Binding="{Binding IsDefault}" Value="True">
                <Setter Property="Background" Value="#1E3A5F" />
            </DataTrigger>
            <!-- BT connected (not default) also gets blue bubble -->
            <MultiDataTrigger>
                <MultiDataTrigger.Conditions>
                    <Condition Binding="{Binding IsBluetooth}" Value="True" />
                    <Condition Binding="{Binding IsBluetoothConnected}" Value="True" />
                </MultiDataTrigger.Conditions>
                <Setter Property="Background" Value="#1E3A5F" />
            </MultiDataTrigger>
        </Style.Triggers>
    </Style>

    <!-- Icon glyph colour -->
    <Style x:Key="DeviceIconTextStyle" TargetType="TextBlock">
        <Setter Property="Foreground" Value="#555555" />
        <Style.Triggers>
            <DataTrigger Binding="{Binding IsDefault}" Value="True">
                <Setter Property="Foreground" Value="#4A90D9" />
            </DataTrigger>
            <MultiDataTrigger>
                <MultiDataTrigger.Conditions>
                    <Condition Binding="{Binding IsBluetooth}" Value="True" />
                    <Condition Binding="{Binding IsBluetoothConnected}" Value="True" />
                </MultiDataTrigger.Conditions>
                <Setter Property="Foreground" Value="#4A90D9" />
            </MultiDataTrigger>
        </Style.Triggers>
    </Style>

    <!-- Subtitle below device name -->
    <Style x:Key="DeviceSubtitleStyle" TargetType="TextBlock">
        <Setter Property="Text" Value="Playback Device" />
        <Setter Property="Foreground" Value="#555555" />
        <Style.Triggers>
            <DataTrigger Binding="{Binding IsDefault}" Value="True">
                <Setter Property="Text" Value="Currently active" />
                <Setter Property="Foreground" Value="#4A90D9" />
            </DataTrigger>
            <MultiDataTrigger>
                <MultiDataTrigger.Conditions>
                    <Condition Binding="{Binding IsBluetooth}" Value="True" />
                    <Condition Binding="{Binding IsBluetoothConnected}" Value="True" />
                    <Condition Binding="{Binding IsDefault}" Value="False" />
                </MultiDataTrigger.Conditions>
                <Setter Property="Text" Value="Bluetooth · Connected" />
                <Setter Property="Foreground" Value="#4A90D9" />
            </MultiDataTrigger>
            <MultiDataTrigger>
                <MultiDataTrigger.Conditions>
                    <Condition Binding="{Binding IsBluetooth}" Value="True" />
                    <Condition Binding="{Binding IsBluetoothConnected}" Value="False" />
                    <Condition Binding="{Binding IsConnecting}" Value="False" />
                </MultiDataTrigger.Conditions>
                <Setter Property="Text" Value="Bluetooth · Click to connect" />
                <Setter Property="Foreground" Value="#666666" />
            </MultiDataTrigger>
            <DataTrigger Binding="{Binding IsConnecting}" Value="True">
                <Setter Property="Text" Value="Connecting…" />
                <Setter Property="Foreground" Value="#888888" />
            </DataTrigger>
            <DataTrigger Binding="{Binding IsConnectionFailed}" Value="True">
                <Setter Property="Text" Value="Connection failed" />
                <Setter Property="Foreground" Value="#E05050" />
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <!-- ══ Settings device card ════════════════════════════════════════════════ -->

    <Style x:Key="SettingsCardButtonStyle" TargetType="Button">
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Card" Background="#222222" BorderBrush="#2E2E2E"
                            BorderThickness="1" CornerRadius="10" Padding="14,12">
                        <ContentPresenter />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Card" Property="Background" Value="#2A2A2A" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Card" Property="Background" Value="#1E1E1E" />
                        </Trigger>
                        <DataTrigger Binding="{Binding IsEnabled}" Value="True">
                            <Setter TargetName="Card" Property="Background" Value="#162033" />
                            <Setter TargetName="Card" Property="BorderBrush" Value="#4A90D9" />
                            <Setter TargetName="Card" Property="BorderThickness" Value="1.5" />
                        </DataTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="SettingsIconBorderStyle" TargetType="Border">
        <Setter Property="Background" Value="#2A2A2A" />
        <Style.Triggers>
            <DataTrigger Binding="{Binding IsEnabled}" Value="True">
                <Setter Property="Background" Value="#1E3A5F" />
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="SettingsIconTextStyle" TargetType="TextBlock">
        <Setter Property="Foreground" Value="#555555" />
        <Style.Triggers>
            <DataTrigger Binding="{Binding IsEnabled}" Value="True">
                <Setter Property="Foreground" Value="#4A90D9" />
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="SettingsNameTextStyle" TargetType="TextBlock">
        <Setter Property="Foreground" Value="#888888" />
        <Style.Triggers>
            <DataTrigger Binding="{Binding IsEnabled}" Value="True">
                <Setter Property="Foreground" Value="#EBEBEB" />
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="SettingsCheckStyle" TargetType="TextBlock">
        <Setter Property="Foreground" Value="Transparent" />
        <Style.Triggers>
            <DataTrigger Binding="{Binding IsEnabled}" Value="True">
                <Setter Property="Foreground" Value="#4A90D9" />
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <!-- ══ Volume slider ════════════════════════════════════════════════════════ -->

    <Style x:Key="VolumeSliderStyle" TargetType="Slider">
        <Setter Property="Minimum" Value="0" />
        <Setter Property="Maximum" Value="1" />
        <Setter Property="SmallChange" Value="0.02" />
        <Setter Property="LargeChange" Value="0.1" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Slider">
                    <Grid VerticalAlignment="Center" Height="16">
                        <Track x:Name="PART_Track">
                            <Track.DecreaseRepeatButton>
                                <RepeatButton Command="Slider.DecreaseLarge" Focusable="False">
                                    <RepeatButton.Template>
                                        <ControlTemplate TargetType="RepeatButton">
                                            <Border Background="#4A90D9" Height="3" CornerRadius="1.5" />
                                        </ControlTemplate>
                                    </RepeatButton.Template>
                                </RepeatButton>
                            </Track.DecreaseRepeatButton>
                            <Track.Thumb>
                                <Thumb Focusable="False">
                                    <Thumb.Template>
                                        <ControlTemplate TargetType="Thumb">
                                            <Ellipse Width="11" Height="11" Fill="#4A90D9">
                                                <Ellipse.Style>
                                                    <Style TargetType="Ellipse">
                                                        <Style.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter Property="Fill" Value="#6AAAE8" />
                                                            </Trigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Ellipse.Style>
                                            </Ellipse>
                                        </ControlTemplate>
                                    </Thumb.Template>
                                </Thumb>
                            </Track.Thumb>
                            <Track.IncreaseRepeatButton>
                                <RepeatButton Command="Slider.IncreaseLarge" Focusable="False">
                                    <RepeatButton.Template>
                                        <ControlTemplate TargetType="RepeatButton">
                                            <Border Background="#2E2E2E" Height="3" CornerRadius="1.5" />
                                        </ControlTemplate>
                                    </RepeatButton.Template>
                                </RepeatButton>
                            </Track.IncreaseRepeatButton>
                        </Track>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
